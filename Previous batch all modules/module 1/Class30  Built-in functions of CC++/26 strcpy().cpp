#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef double dl;

#define endl "\n"
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define fraction() cout.unsetf(ios::floatfield); cout.precision(10); cout.setf(ios::fixed,ios::floatfield);

#define mem(a, b) memset(a, b, sizeof(a))
#define sqr(a) ((a) * (a))


const int mx = 1e5;
int a[mx];
int cnt[100000123];

int main()
{
    optimize();

    char s1[] = "Hello";
    char s2[] = " Riaz";

    cout<< strcpy(s1, s2) << endl; //strcpy(destination, source);

    //strcpy() function is used to copy the string from one to another.
    //destination string should be large enough to store the source string.

   //extra note:

   // string er kkhetre strcpy() use kora jay na, strcpy() use kora jay char array er jonno.
   

    return 0;
}
